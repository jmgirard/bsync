% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surrogate_analysis.R
\name{wcc_surrogate}
\alias{wcc_surrogate}
\title{Calculate Surrogate Windowed Cross-Correlations}
\usage{
wcc_surrogate(
  x,
  y,
  y_surrogates,
  time = NULL,
  window_size,
  lag_max,
  window_increment = 1,
  lag_increment = 1,
  na.rm = TRUE
)
}
\arguments{
\item{x}{A numeric vector containing a time series.}

\item{y}{A numeric vector containing a time series.}

\item{y_surrogates}{A matrix of surrogate time series for `y` (columns are surrogates).}

\item{time}{An optional numeric vector representing the timestamps for the data. Default is `NULL`.}

\item{window_size}{A positive integer indicating the size of each window.}

\item{lag_max}{A positive integer indicating the maximum lag to try.}

\item{window_increment}{A positive integer indicating the window shift increment. Default is 1.}

\item{lag_increment}{A positive integer indicating the lag shift increment. Default is 1.}

\item{na.rm}{A logical indicating whether to remove missing values. Default is `TRUE`.}
}
\value{
A list object of class "wcc_surr".
}
\description{
Calculate Surrogate Windowed Cross-Correlations
}
\details{
The p-value is the proportion of surrogates whose aggregate statistic is **at least as
large as** the observed statistic. The aggregate is `mean(abs(Fisher's Z))` — the same
quantity stored in `wcc_res$fisher_z` — computed identically on both the observed data
and every surrogate, so the null distribution and the observed value are directly
comparable.
}
