% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autotune.R
\name{select_specification}
\alias{select_specification}
\title{Select the Best Specification from a Multi-Dyad Multiverse}
\usage{
select_specification(mv_list, sig_pct = 0.5, iqr_penalty = 0.5)
}
\arguments{
\item{mv_list}{A list of `bsync_multiverse` objects, all run with the same
parameter grid (i.e., all produced by [synchrony_multiverse()] with
identical `window_sec`, `lag_sec`, `increment_pct`, `statistic`, and
`surrogate_method` arguments).}

\item{sig_pct}{Minimum proportion of dyads in which a cell must be
significant (p < .05) to pass the detectability gate. Default `0.5`.}

\item{iqr_penalty}{Penalty weight on cross-dyad IQR of ES in the score
`median(ES) - iqr_penalty * IQR(ES)`. Default `0.5`.}
}
\value{
A list with `best_row` (one-row tibble from the grid), `sig_rate`,
  `median_es`, `iqr_es`, `score`, and `n_gated` for the selected cell.
}
\description{
Applies the gated stability-penalized selection rule to a list of
`bsync_multiverse` objects (one per dyad) and returns the winning cell.
}
\examples{
\donttest{
# Build one multiverse per dyad, then pick the most robust specification.
# Small surrogate count for a fast example; use >= 1000 for reporting.
mv_list <- lapply(seq_len(3), function(i) {
  synchrony_multiverse(
    x = sim_dyad$x_A,
    y = sim_dyad$x_B,
    estimator = "wcc",
    sample_rate = 80,
    window_sec = c(1, 2, 4),
    lag_sec = 1,
    n_surrogates = 30
  )
})
select_specification(mv_list)
}
}
\seealso{
[autotune_wcc()], [synchrony_multiverse()]
}
