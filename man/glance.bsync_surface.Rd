% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{glance.bsync_surface}
\alias{glance.bsync_surface}
\title{One-row summary of a bsync_surface object}
\usage{
\method{glance}{bsync_surface}(x, ...)
}
\arguments{
\item{x}{A `bsync_surface` object (`wcc_res`, `wdtw_res`, or `wgranger_res`).}

\item{...}{Additional arguments (not used).}
}
\value{
A one-row [tibble::tibble()].
}
\description{
Returns a single-row tibble with the aggregate statistic(s) and key settings
so multiple estimator runs can be compared with [dplyr::bind_rows()].
}
\details{
For WCC/WDTW the aggregate is a single named column (`mean_abs_z`, `peak`,
or `mean_distance`). For Granger, `f_xy` and `f_yx` are separate columns.
}
\examples{
wcc_res <- wcc(sim_dyad$x_A, sim_dyad$x_B, window_size = 96, lag_max = 10)
# One-row summary: aggregate statistic(s) + key settings
glance(wcc_res)
}
\seealso{
[tidy.bsync_surface()], [as_tibble.bsync_surface()]
}
